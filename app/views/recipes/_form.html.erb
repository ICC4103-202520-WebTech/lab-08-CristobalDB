<%= form_with model: @recipe, local: true, class: "needs-validation" do |f| %>
  <div class="row">
    <div class="col-md-6 mb-3">
      <%= f.label :title, "Título de la receta", class: "form-label" %>
      <%= f.text_field :title, placeholder: "Ej. Panqueques", class: "form-control", required: true %>
    </div>

    <div class="col-md-3 mb-3">
      <%= f.label :cook_time, "Tiempo de cocción (minutos)", class: "form-label" %>
      <%= f.number_field :cook_time, placeholder: "Ej. 45", min: 0, class: "form-control", required: true %>
    </div>

    <div class="col-md-3 mb-3">
      <%= f.label :difficulty, "Dificultad", class: "form-label" %>
      <%= f.select :difficulty, ["Fácil", "Media", "Difícil"], include_blank: "Seleccionar...", class: "form-select", required: true %>
    </div>
  </div>

  <div class="mb-4">
    <%= f.label :instructions, "Instrucciones", class: "form-label" %>
    <%= f.rich_text_area :instructions, class: "form-control" %>
  </div>

  <div class="d-flex justify-content-end">
    <%= f.submit "💾 Guardar", class: "btn btn-primary px-4" %>
  </div>
<% end %>
